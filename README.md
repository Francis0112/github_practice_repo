# header
for existing repo
* git init
* git pull
* git commit -a -m "message"
* git push
for new repo
* git clone
* git status
* git add
* git commit -m "message"
* git push origin master

## sub header
hello world 
